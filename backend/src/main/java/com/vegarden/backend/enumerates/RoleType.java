/**
 * This enumeration represents the different types of roles in the
 * Vegarden platform.
 * The RoleType enumeration provides predefined role types such as
 * ROLE_ADMIN, ROLE_MODERATOR, and ROLE_USER.
 * These role types are used to assign different levels of access and
 * permissions to users.
 */

package com.vegarden.backend.enumerates;

public enum RoleType {
    ROLE_ADMIN,
    ROLE_MODERATOR,
    ROLE_USER
}
